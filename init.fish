ln -s (pwd)/alacritty $HOME/.config/alacritty
ln -s (pwd)/bat $HOME/.config/bat
ln -s (pwd)/fish $HOME/.config/fish
ln -s (pwd)/helix $HOME/.config/helix
ln -s (pwd)/htop $HOME/.config/htop

# TODO: make so symlinks don't break if you move this folder 
# TODO: find reasonably secure way to setup git config and stuff

