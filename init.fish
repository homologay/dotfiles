# symlink config files:

ln -s alacritty $HOME/.config/alacritty
ln -s bat $HOME/.config/bat
ln -s fish $HOME/.config/fish
ln -s helix $HOME/.config/helix
ln -s htop $HOME/.config/htop

# TODO: find reasonably secure way to setup git config and stuff